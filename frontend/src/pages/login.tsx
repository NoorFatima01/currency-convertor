import LoginForm from "../forms/login-form";

const Login = () => {
  return (
    <section>
      <h2  className="font-bold text-3xl m-4">Log in Form</h2>
      <LoginForm />
    </section>
  );
};

export default Login;
